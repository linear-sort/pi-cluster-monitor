[pytest]
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    unit: Fast, isolated tests for core logic
    integration: API and integration behavior tests
    slow: Longer-running tests (optional on full/manual runs)
